const container = {
  display: 'inline-block'
}

const content = {
  position: 'relative',
  zIndex: 11
}

const overlay = {
  position: 'fixed',
  top: 0,
  left: 0,
  width: '100%',
  height: '100%',
  zIndex: 10
}

const trigger = {
  position: 'relative',
  zIndex: 11
}

export {container, content, overlay, trigger}
